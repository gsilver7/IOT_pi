import WeatherDisplay from "../components/WeatherDisplay";
import Grid from "../components/Grid";

const Home = () => {
  return (
    <div>
      <WeatherDisplay />
      <Grid />
    </div>
  );
};

export default Home;
